- name: green
  size: large
  columns: ['id', 'vendor_id','lpep_pickup_datetime','lpep_dropoff_datetime','store_and_fwd_flag','RateCodeID','pickup_longitude','pickup_latitude','dropoff_longitude','dropoff_latitude','passenger_count','trip_distance','fare_amount','extra','mta_tax','tip_amount','tolls_amount','ehail_fee','improvement_surcharge','total_amount', 'payment_type','trip_type']
  schema: |
    id serial primary key,
    vendor_id integer default null,
    lpep_pickup_datetime datetime default null,
    lpep_dropoff_datetime datetime default null,
    store_and_fwd_flag varchar(1) default null,
    RateCodeID integer default null,
    pickup_longitude numeric default null,
    pickup_latitude numeric default null,
    dropoff_longitude numeric default null,
    dropoff_latitude numeric default null,
    passenger_count integer default null,
    trip_distance numeric default null,
    fare_amount numeric default null,
    extra text default null,
    mta_tax text default null,
    tip_amount text default null,
    tolls_amount text default null,
    ehail_fee text default null,
    improvement_surcharge text default null,
    total_amount text default null,
    payment_type text default null,
    trip_type integer default null

- name: yellow
  size: large
  columns: ['id','vendor_id','tpep_pickup_datetime','tpep_dropoff_datetime','passenger_count','trip_distance','pickup_longitude','pickup_latitude','rate_code_id','store_and_fwd_flag', 'dropoff_longitude','dropoff_latitude', 'payment_type','fare_amount','extra','mta_tax','tip_amount','tolls_amount', 'improvement_surcharge','total_amount','pickup_location_id','dropoff_location_id','congestion_surcharge']
  schema: |
    id serial primary key,
    vendor_id integer default null,
    tpep_pickup_datetime text,
    tpep_dropoff_datetime text,
    passenger_count text,
    trip_distance text,
    pickup_longitude numeric,
    pickup_latitude numeric,
    rate_code_id text,
    store_and_fwd_flag text,
    dropoff_longitude numeric,
    dropoff_latitude numeric,
    payment_type text,
    fare_amount text,
    extra text,
    mta_tax text,
    tip_amount text,
    tolls_amount text,
    improvement_surcharge text,
    total_amount text,
    pickup_location_id text,
    dropoff_location_id text,
    congestion_surcharge text

- name: fhv_ 
  size: large
  columns: ['Dispatching_base_num','Pickup_date','locationID']
  schema: |
    Dispatching_base_num varchar(6) default null,
    Pickup_date datetime default null,
    locationID integer default null

    
- name: fhvhv 
  size: large
  columns: ['Hvfhs_license_num','Dispatching_base_num','Pickup_datetime', 'DropOff_datetime', 'PULocationID', 'DOLocationID', 'SR_Flag']
  schema: |
    Hvfhs_license_num varchar(6) default null,
    Dispatching_base_num varchar(6) default null,
    Pickup_datetime datetime default null,
    DropOff_datetime datetime default null,
    PULocationID integer default null,
    DOLocationID integer default null,
    SR_Flag boolean default 0
